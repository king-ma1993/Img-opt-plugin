pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven {
            url "ZrgkLocalRepo"
        }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            url "ZrgkLocalRepo"
        }
    }
}
rootProject.name = "Img-opt-gradle-plugin"
include ':app'
include ':img-opt'
